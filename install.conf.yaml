- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc
    ~/.vimrc: vimrc
    ~/.babelrc: babelrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
